plugins {
    // Lets Gradle auto-download the JDK 25 toolchain that Project Zomboid needs.
    id("org.gradle.toolchains.foojay-resolver-convention") version "0.9.0"
}

rootProject.name = "spiffo-gradle"
